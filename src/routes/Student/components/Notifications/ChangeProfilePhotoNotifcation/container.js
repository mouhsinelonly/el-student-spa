import { connect } from 'react-redux'

import ChangeProfilePhotoNotifcation from './ChangeProfilePhotoNotifcation'

const mapActionCreators = {
}

const mapStateToProps = (state) => {
  return {}
}

export default connect(mapStateToProps, mapActionCreators)(ChangeProfilePhotoNotifcation)
